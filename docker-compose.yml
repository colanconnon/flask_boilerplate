version: '2'
services:
  celery:
    build: .
    command: celery worker -l info -A celery_setup.celery
    volumes:
      - '.:/src'
  redis:
    image: 'redis:3.0-alpine'
    command: redis-server --requirepass devpassword
    volumes:
      - 'redis:/var/lib/redis/data'
    ports:
      - '6379:6379'
  db:
    restart: always
    image: postgres
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
        POSTGRES_PASSWORD: secret
        POSTGRES_USER: flask
        POSTGRES_DB: flask_db
    ports:
      - "54324:5432"
  web:
    build: .
    restart: always
    command: bash ./deploy/start.sh
    environment: 
      - FLASK_ENV=development
    volumes:
      - .:/src
    ports:
      - "8000:8000"
    depends_on:
      - db
  nginx:
    restart: always
    build: ./nginx/.
    ports:
        - "80:80"
    volumes:
        - /www/static
    volumes_from:
        - web
    links:
        - web:web
volumes:
  postgres-data:
    driver: local
  redis:
    driver: local
